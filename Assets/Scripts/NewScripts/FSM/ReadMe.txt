针对有限状态机模块
对不同的对象创建不同的状态机进行管理，继承并实现FsmBase抽象类,然后需要对对象创建不同的状态，继承自FsmState<T>同时制定该状态是属于哪个状态机管理的，所以T为对应的状态机类型。
创建好这些基础类后，先new一个状态机管理器，然后将所有的状态new出来，之后再讲状态机添加到状态机管理器中，以便统一管理，然后调用状态机的开启方法开启状态机。利用FireWork进行
状态之间的切换。